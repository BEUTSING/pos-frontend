name: pos
description: "Système de Gestion de Point de Vente - Restaurant"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Requêtes HTTP vers l'API Symfony
  http: ^1.2.0

  # Stockage local (token JWT, infos utilisateur)
  shared_preferences: ^2.2.2

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
   - assets/logo.png
